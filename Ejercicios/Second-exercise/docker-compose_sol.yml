services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./notas:/app/notas
    networks:
      - app_network

  redis:
    image: redis:7-alpine
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
